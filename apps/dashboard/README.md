# apps/dashboard

Streamlit ingestion dashboard for HybridRAG.

## Run

From this directory:

```powershell
streamlit run streamlit_app.py
```

By default the UI points at `http://127.0.0.1:8000/api/ingest/`.
